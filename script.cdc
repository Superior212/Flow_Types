import SomeContract from 0x05

pub fun main() {
  /**************/
  /*** AREA 4 ***/
  /***********
    a (pub(set)): Readable, not writable.
    b (pub): Readable, not writable.
    c (access(contract)): Not accessible.
    d (access(self)): Not accessible.
    Functions
    publicFunc: Callable.
    contractFunc: Not callable.
    privateFunc: Not callable.
  ***/
}
